{{!-- `.empty` is the blog engine's name for this line and carries its colour.

A failed SEARCH and an empty archive are not the same sentence, so the caller says which. --}}
<p class="empty">{{#if message}}{{message}}{{else}}No posts here yet.{{/if}}</p>
